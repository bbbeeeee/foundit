extends layout

block css
	link(rel='stylesheet', href='/css/found_list.css')

block content
	#founds
		each found in foundlist
			.foundobject
				.pic
				a(href="/found/#{found._id}")= found.description
